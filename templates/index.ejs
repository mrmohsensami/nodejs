<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog | <%= title %>
    </title>
</head>

<body>

    <% const name='Page' %>
        <h1>Home <%= name %>
        </h1>
        <hr>
        <% if(blogs.length> 0) { %>
            <% blogs.forEach(element=> {%>
                <h3>
                    <%= element.title %>
                </h3>
                <p>
                    <%= element.snippet %>
                </p>
                <%}) %>
                    <% } else { %>
                        <p>no blogs</p>
                        <% } %>
</body>

</html>